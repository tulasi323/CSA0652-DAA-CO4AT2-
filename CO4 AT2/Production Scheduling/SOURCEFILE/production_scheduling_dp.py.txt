# Production Scheduling using Greedy Strategy

jobs = [4, 2, 6, 3, 5]

jobs.sort()

time = 0
total_completion_time = 0

for job in jobs:
    time += job
    total_completion_time += time

print("Scheduled Jobs:", jobs)
print("Total Completion Time =", total_completion_time)